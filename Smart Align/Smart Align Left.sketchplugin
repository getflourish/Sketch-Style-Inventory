// Aligns layers to their parent group’s left edge (cmd alt ←)

for (var i = 0; i < selection.count(); i++) {
	var layer = selection[i];
	layer.absoluteRect().setX(layer.parentGroup().absoluteRect().x());
}