// Aligns layers to their parent group’s top edge (cmd alt ↑)

for (var i = 0; i < selection.count(); i++) {
	var layer = selection[i];
	layer.absoluteRect().setY(layer.parentGroup().absoluteRect().y());
}