// Creates a shared object for the selected layers (cmd control option e)

@import '../inventory.js'

onRun();


function onRun () {
    var doc = context.document;
    com.getflourish.symbolInventory.generate();
}